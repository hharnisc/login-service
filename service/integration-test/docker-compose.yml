version: '2'

services:
  integration-tester:
    build: .
    environment:
      - JWT_SECRET=secret
    links:
      - login
  login:
    build: ../..
    links:
      - auth
      - user
    expose:
      - "8080"
  auth:
    image: hharnisc/auth
    environment:
      - RETHINKDB_SERVICE_HOST=rethinkdb
      - RETHINKDB_SERVICE_PORT=28015
      - JWT_SECRET=secret
    links:
      - rethinkdb
    expose:
      - "8080"
  user:
    image: hharnisc/user
    environment:
      - RETHINKDB_SERVICE_HOST=rethinkdb
      - RETHINKDB_SERVICE_PORT=28015
    links:
      - rethinkdb
    expose:
      - "8080"
  rethinkdb:
    image: rethinkdb
    expose:
      - "28015"
